services:
  cross-pdf-rag-backend:
    build:
      context: .
      no_cache: true
      dockerfile: Dockerfile.backend
    image: cross-pdf-rag-backend-image:latest
    ports:
      - "8000:8000"
    restart: always
    container_name: cross-pdf-rag-backend-container